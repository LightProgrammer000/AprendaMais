# Solicita ao usuário um número e o converte para float
valor = float(input('Digite um número: '))

# Verifica se o número digitado é negativo
if valor < 0:

    # Se a condição for verdadeira (valor negativo), imprime a mensagem
    print('O número que você digitou é negativo')

    # Explica que o número é menor que zero
    print('e eu sei disso pois ele é menor que zero')
